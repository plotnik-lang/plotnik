[package]
name = "plotnik-abi-compat"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
plotnik = { path = "../../crates/plotnik-cli", default-features = false, features = ["all-languages"] }
