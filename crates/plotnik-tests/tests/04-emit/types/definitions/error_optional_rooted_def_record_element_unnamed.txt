Bad = (expression_statement
  (identifier) @id
)?
------------------ DIAGNOSTICS -------------------
error: the definition names the optional itself, so each `?` element — an anonymous record of captures — is left without a type name
 --> query.ptk:1:7
  |
1 |   Bad = (expression_statement
  |  _______^
2 | |   (identifier) @id
3 | | )?
  | |__^
  |
help: name the element type in its own definition, then quantify a reference to it: `Elem = ...` and `(Elem)?`
