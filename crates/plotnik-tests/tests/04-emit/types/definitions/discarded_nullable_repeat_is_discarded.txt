MaybeId = (identifier)?

Q = (program
  (expression_statement
    (array
      (MaybeId)* @_
    )
  )
)
------------------- TYPESCRIPT -------------------
export type Q = undefined;
---------------------- RUST ----------------------
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q;
