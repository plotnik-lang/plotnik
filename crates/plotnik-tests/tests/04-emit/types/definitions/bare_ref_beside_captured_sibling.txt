A = [
  X: (expression_statement) @s
]

Q = (program
  (A)
  (debugger_statement) @name
)
------------------- TYPESCRIPT -------------------
export type A =
  | { $tag: "X"; $data: { s: Node } };

export interface Q {
  name: Node;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum A<'t> {
    X { s: Node<'t> },
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub name: Node<'t>,
}
