A = [
  Base: (identifier) @id
  Nest: (array (B) @b)
]

B = [
  Leaf: (number) @n
  Wrap: (parenthesized_expression (A) @a)
]

Q = (program (expression_statement (A) @root))
------------------- TYPESCRIPT -------------------
export type B =
  | { $tag: "Leaf"; $data: { n: Node } }
  | { $tag: "Wrap"; $data: { a: A } };

export type A =
  | { $tag: "Base"; $data: { id: Node } }
  | { $tag: "Nest"; $data: { b: B } };

export interface Q {
  root: A;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum B<'t> {
    Leaf { n: Node<'t> },
    Wrap { a: ::std::boxed::Box<A<'t>> },
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum A<'t> {
    Base { id: Node<'t> },
    Nest { b: ::std::boxed::Box<B<'t>> },
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub root: A<'t>,
}
