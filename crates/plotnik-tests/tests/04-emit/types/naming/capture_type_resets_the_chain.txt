Foo = (function_declaration
  body: (statement_block
    {
      (expression_statement
        {
          (identifier) @v
        } @inner
      )
    } @outer :: Bar
  )
)
------------------- TYPESCRIPT -------------------
export interface Foo {
  outer: Bar;
}

export interface Bar {
  inner: BarInner;
}

export interface BarInner {
  v: Node;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Foo<'t> {
    pub outer: Bar<'t>,
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Bar<'t> {
    pub inner: BarInner<'t>,
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct BarInner<'t> {
    pub v: Node<'t>,
}
