Q = (identifier) @x :: Node
------------------ DIAGNOSTICS -------------------
warning: this capture already has type `Node`; naming it `Node` has no effect
 --> query.ptk:1:24
  |
1 | Q = (identifier) @x :: Node
  |                        ^^^^
------------------- TYPESCRIPT -------------------
export interface Q {
  x: Node;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub x: Node<'t>,
}
