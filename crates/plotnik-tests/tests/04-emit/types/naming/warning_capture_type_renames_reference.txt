Foo = (expression_statement (call_expression) @call)

Q = (program
  (Foo) @val :: Renamed
)
------------------ DIAGNOSTICS -------------------
warning: this capture already has type `Foo`; naming it `Renamed` won't have an effect
 --> query.ptk:4:17
  |
4 |   (Foo) @val :: Renamed
  |                 ^^^^^^^
------------------- TYPESCRIPT -------------------
export interface Foo {
  call: Node;
}

export interface Q {
  val: Foo;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Foo<'t> {
    pub call: Node<'t>,
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub val: Foo<'t>,
}
