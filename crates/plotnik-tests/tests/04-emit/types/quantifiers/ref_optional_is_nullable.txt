Row = (pair key: (_) @key value: (_) @val)

Q = (program
  (expression_statement
    (parenthesized_expression (object (Row)? @first))
  )
)
------------------- TYPESCRIPT -------------------
export interface Row {
  key: Node;
  val: Node;
}

export interface Q {
  first: Row | null;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Row<'t> {
    pub key: Node<'t>,
    pub val: Node<'t>,
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub first: ::core::option::Option<Row<'t>>,
}
