A = [
  X: (identifier) @x
  Y: (number) @y
]

Q = [
  (A)
  (number) @b
]
------------------- TYPESCRIPT -------------------
export type A =
  | { $tag: "X"; $data: { x: Node } }
  | { $tag: "Y"; $data: { y: Node } };

export interface Q {
  b: Node | null;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum A<'t> {
    X { x: Node<'t> },
    Y { y: Node<'t> },
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub b: ::core::option::Option<Node<'t>>,
}
