Q = (function_declaration
  name: (identifier) @name
  body: (statement_block) @body
) @func :: FunctionInfo
------------------- TYPESCRIPT -------------------
export interface Q {
  body: Node;
  func: FunctionInfo;
  name: Node;
}

export type FunctionInfo = Node;
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub func: FunctionInfo<'t>,
    pub name: Node<'t>,
    pub body: Node<'t>,
}

pub type FunctionInfo<'t> = Node<'t>;
