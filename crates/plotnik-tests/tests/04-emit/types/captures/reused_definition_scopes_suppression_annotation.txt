Foo = {(comment) @x}

Q = (program
  {
    (Foo) @suppressed :: text
    (Foo) @kept
  }
)
------------------ DIAGNOSTICS -------------------
warning: capture type `text` replaces the record value captured by `@suppressed` with source text
 --> query.ptk:5:11
  |
1 | Foo = {(comment) @x}
  |                  -- this captured value is suppressed by `@suppressed :: text`
...
5 |     (Foo) @suppressed :: text
  |           ^^^^^^^^^^^^^^^^^^^
  |
help: remove `:: text` to return the record value instead
------------------- TYPESCRIPT -------------------
export interface Foo {
  x: Node;
}

export interface Q {
  kept: Foo;
  suppressed: string;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Foo<'t> {
    pub x: Node<'t>,
}

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t, 's> {
    pub suppressed: &'s str,
    pub kept: Foo<'t>,
}
