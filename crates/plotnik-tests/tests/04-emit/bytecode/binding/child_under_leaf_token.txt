Q = (identifier (_))
------------------ DIAGNOSTICS -------------------
error: `identifier` is a leaf token and has no child nodes
 --> query.ptk:1:17
  |
1 | Q = (identifier (_))
  |      ---------- ^^^
  |      |
  |      `identifier`
  |
help: a leaf token's content is its text. Match `(identifier)` directly or add a text predicate to `(identifier)`
