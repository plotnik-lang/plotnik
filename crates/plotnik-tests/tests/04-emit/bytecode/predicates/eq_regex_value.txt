Q = (identifier == /foo/) @id
------------------ DIAGNOSTICS -------------------
error: predicate `identifier == /foo/` uses `==`, which requires a quoted string, but supplies a regex value
 --> query.ptk:1:17
  |
1 | Q = (identifier == /foo/) @id
  |      ---------- ^^^^^^^^
  |      |
  |      predicate applies to `identifier`
  |
help: change the value in `== /foo/` to a quoted string, or choose an operator that accepts a regex value
