Expr = [
  Lit: (number) @n
  Rec: (parenthesized_expression (Expr))
]

Q = (expression_statement (Expr)*)
------------------- TYPESCRIPT -------------------
export type Expr =
  | { $tag: "Lit"; $data: { n: Node } }
  | { $tag: "Rec" };

export type Q = undefined;
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum Expr<'t> {
    Lit { n: Node<'t> },
    Rec,
}

// `Q` matches without producing data; no output type.
