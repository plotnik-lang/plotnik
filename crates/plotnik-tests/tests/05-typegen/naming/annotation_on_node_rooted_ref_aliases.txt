A = (identifier)
Q = (program (expression_statement (A) @x :: Foo))
------------------- TYPESCRIPT -------------------
export type Foo = Node;

export interface Q {
  x: Foo;
}

export type A = Node;
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

pub type A<'t> = Node<'t>;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub x: Foo<'t>,
}

pub type Foo<'t> = Node<'t>;
