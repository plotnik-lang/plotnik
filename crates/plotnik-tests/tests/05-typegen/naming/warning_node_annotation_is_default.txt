Q = (identifier) @x :: Node
------------------ DIAGNOSTICS -------------------
warning: this type annotation `Node` is already the default type; omit it
 --> query.ptk:1:24
  |
1 | Q = (identifier) @x :: Node
  |                        ^^^^
------------------- TYPESCRIPT -------------------
export interface Q {
  x: Node;
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub x: Node<'t>,
}
