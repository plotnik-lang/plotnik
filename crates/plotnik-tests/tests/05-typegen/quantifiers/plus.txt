Q = (identifier)+ @ids
------------------- TYPESCRIPT -------------------
export interface Q {
  ids: [Node, ...Node[]];
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub ids: ::std::vec::Vec<Node<'t>>,
}
