Item = [A: (expression_statement) @s]
Q = (program (Item)+)
------------------- TYPESCRIPT -------------------
export type Item =
  | { $tag: "A"; $data: { s: Node } };

export type Q = Node;
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum Item<'t> {
    A { s: Node<'t> },
}

pub type Q<'t> = Node<'t>;
