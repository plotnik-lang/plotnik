Ids = (identifier)+

Q = (program
  (expression_statement
    (array
      (Ids) @ids
      (Ids)* @runs
    )
  )
)
--------------------- TYPES ----------------------
export interface Q {
  ids: Ids;
  runs: Ids[];
}

export type Ids = [Node, ...Node[]];
