A = [X: (expression_statement) @s]

Q = (program
  (A)
  (debugger_statement) @name
)
--------------------- TYPES ----------------------
export interface Q {
  name: Node;
}

export type A =
  | { $tag: "X"; $data: { s: Node } };
