Id = (identifier) @id
Foo = (function_declaration name: (Id))
Bar = (class_declaration name: (Id))
------------------- TYPESCRIPT -------------------
export interface Id {
  id: Node;
}

export type Foo = undefined;

export type Bar = undefined;
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Id<'t> {
    pub id: Node<'t>,
}

// `Foo` matches without producing data; no output type.

// `Bar` matches without producing data; no output type.
