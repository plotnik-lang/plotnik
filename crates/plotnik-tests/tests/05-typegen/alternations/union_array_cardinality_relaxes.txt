Q = [
  {(identifier)+ @xs}
  {(number)* @xs}
]
------------------- TYPESCRIPT -------------------
export interface Q {
  xs: Node[];
}
---------------------- RUST ----------------------
use ::plotnik_rt::Node;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct Q<'t> {
    pub xs: ::std::vec::Vec<Node<'t>>,
}
