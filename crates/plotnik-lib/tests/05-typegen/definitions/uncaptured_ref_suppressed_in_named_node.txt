A = [
  X: (identifier) @x
  Y: (number) @y
]

Q = (expression_statement (A))
--------------------- TYPES ----------------------
export type A =
  | { $tag: "X"; $data: { x: Node } }
  | { $tag: "Y"; $data: { y: Node } };

export type Q = Node;
