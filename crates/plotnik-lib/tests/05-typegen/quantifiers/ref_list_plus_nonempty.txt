Row = (pair key: (_) @key value: (_) @val)
Q = (program (expression_statement (parenthesized_expression (object (Row)+ @rows))))
--------------------- TYPES ----------------------
export interface Row {
  key: Node;
  val: Node;
}

export interface Q {
  rows: [Row, ...Row[]];
}
