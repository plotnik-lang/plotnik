Foo = (expression_statement (call_expression) @call)
Q = (program (Foo) @val :: Foo)
------------------ DIAGNOSTICS -------------------
warning: this type annotation restates the type's own name; omit it
 --> query.ptk:2:28
  |
2 | Q = (program (Foo) @val :: Foo)
  |                            ^^^
--------------------- TYPES ----------------------
export interface Foo {
  call: Node;
}

export interface Q {
  val: Foo;
}
