A = [
  X: (identifier)
  Y: (number)
]

Q = (program
  (expression_statement (A) @a)
  (debugger_statement) @name
)
--------------------- TYPES ----------------------
export type A =
  | { $tag: "X" }
  | { $tag: "Y" };

export interface Q {
  a: A;
  name: Node;
}
