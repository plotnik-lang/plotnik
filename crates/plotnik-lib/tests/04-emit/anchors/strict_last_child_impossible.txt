Q = (array (identifier) .!)
------------------ DIAGNOSTICS -------------------
error: with strict adjacency (`.!`), the last child of `array` must be `identifier`, but `array` ends with `"]"`
 --> query.ptk:1:6
  |
1 | Q = (array (identifier) .!)
  |      ^^^^^
  |
help: `.!` allows nothing between — not even anonymous tokens or comments; the soft anchor `.` skips those: `(array (identifier) .)`
