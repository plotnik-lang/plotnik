Q = (if
  condition: (expr)
  (then_block)
  else: (else_block)
)
==== cst ====
# query.ptk
Root
  Def
    Id "Q"
    Equals "="
    Tree
      ParenOpen "("
      Id "if"
      Field
        Id "condition"
        Colon ":"
        Tree
          ParenOpen "("
          Id "expr"
          ParenClose ")"
      Tree
        ParenOpen "("
        Id "then_block"
        ParenClose ")"
      Field
        Id "else"
        Colon ":"
        Tree
          ParenOpen "("
          Id "else_block"
          ParenClose ")"
      ParenClose ")"
==== ast ====
# query.ptk
Root
  Def Q
    NamedNode if
      FieldPattern condition:
        NamedNode expr
      NamedNode then_block
      FieldPattern else:
        NamedNode else_block
