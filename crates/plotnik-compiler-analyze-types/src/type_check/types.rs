//! The type-system data model lives in `plotnik-compiler-core`; re-exported here as
//! the `types` module the type-check pass internals build against.

pub use plotnik_compiler_core::type_shape::*;
